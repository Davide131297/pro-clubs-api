{
  "name": "pro-clubs-api-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Express API proxy for EA FC Pro Clubs endpoints.",
  "type": "module",
  "scripts": {
    "check": "node --check src/config.js && node --check src/eaApi.js && node --check src/server.js",
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^5.1.0",
    "helmet": "^8.1.0"
  }
}
